// Seleciona o container principal
const authBox = document.getElementById('authBox');

// Função para alternar entre os formulários de login e registro
function toggleForm() {
    authBox.classList.toggle('ativo');
}